import sys
import json

def main():
    varsText = sys.argv[1]
    varsJson = json.loads(varsText)

    # Configurar las variables de entorno
    for key, value in varsJson.items():
        print(f'{key}: {value}')

if __name__ == "__main__":
    main()

